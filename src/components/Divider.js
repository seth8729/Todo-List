import React from 'react';
import './Divider.css';
import { Box } from '@mui/material';

// Divider 元件
function Divider() {
  return (
    <Box className='Divider' borderRadius={1}/>
  );
};

export default Divider;
